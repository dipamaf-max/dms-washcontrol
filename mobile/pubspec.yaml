name: dms_washcontrol
description: DMS WashControl - application mobile pour les stations de lavage automobile.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  provider: ^6.1.2
  go_router: ^14.6.2
  mobile_scanner: ^6.0.2
  intl: ^0.19.0
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
